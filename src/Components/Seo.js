import React from "react";
import { Helmet } from "react-helmet";
import { StaticQuery, graphql } from 'gatsby'

export const Seo = ({ title, description, author, keywords, image, siteUrl }) => {

}

